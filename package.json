{
  "name": "mangobot-email",
  "version": "1.0.0",
  "description": "Cloudflare Workers email service with Resend API - subscription management and email forwarding",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
